(use system)
(define-system λℵ
  (scheme-file "secret")
  (scheme-file "project")
  (scheme-file "webhooks" depends: '("secret" "project"))
  (scheme-file "websockets" depends: '("secret" "project"))
  (scheme-file "λℵ" depends: '("project" "webhooks" "websockets")))

(load-system λℵ quiet: #t)
